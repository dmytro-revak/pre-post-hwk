{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-html": "node_modules/.bin/pug -o dest/ --pretty src/",
    "build-css": "node_modules/.bin/node-sass src/styles/ -o dest/styles",
    "build": "npm run build-html && npm run build-css"
  },
  "dependencies": {
    "node-sass": "^3.13.0",
    "pug": "^2.0.0-beta6",
    "pug-cli": "^1.0.0-alpha6"
  }
}
